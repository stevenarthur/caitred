%h2 Reset password
- flash.each do |key, value|
  %div{class: flash_class(key)}
    = value
= form_for @user, url: edit_profile_path(@user), builder: Judge::FormBuilder, html: { class: 'form-horizontal js-judge-validate-form'} do |form|
  = hidden_field_tag :form_type, @form_type
  = render partial: 'password_field', locals: { field_name: :password, field_id: 'js-password', field_label: "New Password", form: form }
  = render partial: 'password_field', locals: { field_name: :password_confirmation, field_id: 'js-password_confirmation', field_label: "Confirm Password", form: form }
  .form-group.js-field-group
    .col-sm-offset-2.col-xs-6
      = submit_tag "Update Password", class: 'btn btn-success js-judge-validate'
